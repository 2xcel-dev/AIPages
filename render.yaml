services:
  - type: web
    name: aipages-backend
    runtime: node
    rootDirectory: .
    buildCommand: npm install && npm run build
    startCommand: node dist/index.js
    envVars:
      - key: NODE_ENV
        value: production
